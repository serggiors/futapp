import styled from "styled-components";
import { Link as LinkS } from 'react-scroll';

export const ReservaContainer = styled.section`
    background: #fff;
    padding: 20px 50px;
    position: relative;
    z-index: 1;
    min-height: 90vh;
`